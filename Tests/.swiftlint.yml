disabled_rules:
  - line_length
  - file_length
  - function_body_length
  - type_body_length
  - nesting
  - type_name

opt_in_rules:
  - missing_docs

included:

excluded:
  .build

trailing_comma:
  mandatory_comma: true

vertical_whitespace:
  max_empty_lines: 2
